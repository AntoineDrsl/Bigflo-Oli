{% extends 'base.html.twig' %}

{% block title %}Se connecter{% endblock %}

{% block body %}
<div class="container">
    <form method="post" class="mt-5">
        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        <h1 class="h3 mb-3 font-weight-normal">Se connecter</h1>
        <label for="inputEmail">Email</label>
        <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required autofocus>
        <label for="inputPassword" class="mt-2">Mot de passe</label>
        <input type="password" name="password" id="inputPassword" class="form-control" required>

        <input type="hidden" name="_csrf_token"
            value="{{ csrf_token('authenticate') }}"
        >

        <!-- Si l'utilisateur coche cette case, un cookie va stocket ses infos et il sera connecté pendant 1 semaine (paramètres dans security.yaml) -->
        <div class="checkbox my-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>

        <button class="btn btn-lg btn-primary mt-3" type="submit">
            Se connecter
        </button>
    </form>
    <div class="mt-4">Pas encore de compte? <a href="{{ path('register') }}" class="text-primary">Rejoignez la communauté !</a></div>
</div>
{% endblock %}
